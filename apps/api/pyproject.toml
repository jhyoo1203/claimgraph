[project]
name = "claimgraph-api"
version = "0.1.0"
description = "ClaimGraph FastAPI service"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
]

[tool.uv]
package = false
